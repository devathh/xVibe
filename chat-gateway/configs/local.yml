# global environment of current service
env: dev

# naming of this gateway
app:
  name: xvibe-chat-gateway
  version: v0.0.1

# settings of runner of server
server:
  http:
    host: 0.0.0.0
    port: 7081
    tls:
      enable: true
      server-cert: ./certs/server.crt
      server-key: ./certs/server.key
  read-timeout: 2s
  write-timeout: 2s
  idle-timeout: 30s

# settings of connected services
services:
  xvibe-chat:
    host: chat-service
    port: 50051
    # settings of mtls connection
    tls:
      enable: false
      client-cert: ./gateway-certs/client.crt
      client-key: ./gateway-certs/client.key
      ca-cert: ./gateway-certs/rootCA.pem
